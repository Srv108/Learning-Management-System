@model Learning_Management_System.Models.Dtos.ExamRegistrationDto

@{
    ViewData["Title"] = "Registration Details";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <h3>Registration Details</h3>
            <p class="text-muted">Details for your exam registration.</p>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    <h4>@Model.Title</h4>
                    <p class="text-muted">@Model.SubjectName &middot; @((DateTime)Model.ExamDate).ToString("MMM dd, yyyy")</p>

                    <dl class="row">
                        <dt class="col-sm-3">Status</dt>
                        <dd class="col-sm-9"><span class="badge bg-success">@Model.Status</span></dd>














</div>    </div>        </div>            </div>                </div>
n                    <a class="btn btn-outline-primary" asp-action="Index">Back to Exams</a>                    </dl>                        <dd class="col-sm-9">@((DateTime)Model.CreatedAt).ToString("MMM dd, yyyy HH:mm")</dd>
n                        <dt class="col-sm-3">Registered At</dt>                        <dd class="col-sm-9">@Model.CreatedBy</dd>
n                        <dt class="col-sm-3">Scheduled By</dt>                        <dd class="col-sm-9">@Model.MaxScore</dd>n                        <dt class="col-sm-3">Max Score</dt>