@{
    ViewData["Title"] = "Schedule Exam";
    var coursesJson = ViewBag.CoursesJson as string ?? "[]";
    ViewData["Breadcrumb"] = "<a href='/ExamMvc/Index' style='color:var(--text-muted);text-decoration:none'>Exams</a> <span style='margin:0 6px;color:var(--border-strong)'>›</span> <span class='current'>Schedule Exam</span>";
}

<div class="page-header">
    <div>
        <div class="page-title">Schedule Exam</div>
        <div class="page-sub">Create a new exam for a subject</div>
    </div>
    <a asp-action="Index" class="btn-lms-ghost">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
        Back to Exams
    </a>
</div>

<div style="max-width:560px">
    <div class="lms-card">
        <form method="post" asp-action="Create" style="display:flex;flex-direction:column;gap:18px">
            <div class="lms-form-group">
                <label class="lms-label">Course <span style="color:var(--rose)">*</span></label>
                <select class="lms-select" id="courseSelect" required>
                    <option value="">Select Course</option>
                </select>
            </div>
            <div class="lms-form-group">
                <label class="lms-label">Subject <span style="color:var(--rose)">*</span></label>
                <select class="lms-select" name="subjectId" id="subjectSelect" required disabled>
                    <option value="">Select Course First</option>
                </select>
            </div>
            <div class="lms-form-group">
                <label class="lms-label">Exam Title <span style="color:var(--rose)">*</span></label>
                <input type="text" name="title" class="lms-input" required placeholder="e.g., Midterm Examination" />
            </div>
            <div class="lms-form-group">
                <label class="lms-label">Exam Type <span style="color:var(--rose)">*</span></label>
                <select name="examType" class="lms-select" required>
                    <option value="MIDTERM">Midterm</option>
                    <option value="FINAL">Final</option>
                    <option value="QUIZ">Quiz</option>
                    <option value="PRACTICAL">Practical</option>
                </select>
            </div>
            <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px">
                <div class="lms-form-group">
                    <label class="lms-label">Max Score <span style="color:var(--rose)">*</span></label>
                    <input type="number" name="maxScore" class="lms-input" required min="1" value="100" />
                </div>
                <div class="lms-form-group">
                    <label class="lms-label">Exam Date <span style="color:var(--rose)">*</span></label>
                    <input type="datetime-local" name="examDate" class="lms-input" required id="examDate" />
                </div>
            </div>
            <div style="display:flex;gap:10px;padding-top:4px">
                <button type="submit" class="btn-lms-primary">Schedule Exam</button>
                <a asp-action="Index" class="btn-lms-ghost">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
<script>
    const courses = @Html.Raw(coursesJson);
    const csel = document.getElementById('courseSelect');
    const ssel = document.getElementById('subjectSelect');

    courses.forEach(c => {
        const opt = document.createElement('option');
        opt.value = c.id; opt.textContent = c.title;
        csel.appendChild(opt);
    });

    csel.addEventListener('change', async function() {
        ssel.innerHTML = '<option value="">Loading...</option>';
        ssel.disabled = true;
        if (!this.value) { ssel.innerHTML = '<option value="">Select Course First</option>'; return; }
        const res = await fetch(`/api/subject/course/${this.value}`);
        const subs = await res.json();
        ssel.innerHTML = '<option value="">Select Subject</option>';
        subs.forEach(s => {
            const opt = document.createElement('option');
            opt.value = s.id; opt.textContent = s.name;
            ssel.appendChild(opt);
        });
        ssel.disabled = false;
    });

    const d = new Date();
    d.setDate(d.getDate() + 14);
    document.getElementById('examDate').value = d.toISOString().slice(0, 16);
</script>
}
